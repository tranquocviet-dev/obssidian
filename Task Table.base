views:
  - type: table
    name: Table
    filters:
      and:
        - categories.contains(link("Tasks"))
        - '!status.contains(link("Finished"))'
    order:
      - file.name
      - created
      - status
    columnSize:
      file.name: 1265
